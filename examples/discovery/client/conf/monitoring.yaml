cse:
  metrics:
    apiPath: /metrics      # we can also give api path having prefix "/" ,like /adas/metrics
    enable: true
    enableGoRuntimeMetrics: true
    flushInterval: 10s
    circuitMetricsConsumerNum: 10
tracing:
  collectorType: zipkin
  collectorTarget: http://localhost:9411/api/v1/spans